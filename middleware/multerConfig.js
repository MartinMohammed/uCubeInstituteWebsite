const multer = require("multer");

// const fileStorge = multer.diskStorage()
// const fileFilter =

// image = file input field in req.body
// module.exports = multer({storage: , fileFilter: }).single("image")
